#include<stdio.h>
#include<string.h>
/**
 * _strcat - combines two strings
 * @dest: first string
 * @src: second string
 *
 * Return: Always Success
 */
char *_strcat(char *dest, char *src)
{
	return (0);
}
