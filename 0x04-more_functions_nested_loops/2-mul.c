#include "main.h"
#include <stdio.h>

/**
 * mul - a program that multiplies two integers
 * @a: first number
 * @b: second number
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	return (a * b);
}
